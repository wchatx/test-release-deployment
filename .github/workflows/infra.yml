name: Infra

on:
  push:
    branches:
      - main
    tags:
      - '[0-9]+.[0-9]+.[0-9]+'
    paths:
      - infra/**

  pull_request:
    branches:
      - main
    paths:
      - infra/**

permissions:
  id-token: write
  contents: read
  pull-requests: write

jobs:
  infra:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
